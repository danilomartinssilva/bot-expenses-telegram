services:
  bot:
    build: .
    env_file:
      - .env
    environment:
      - PORT=${PORT:-10000}
    ports:
      - "${PORT:-10000}:${PORT:-10000}"
    volumes:
      - .:/app
      - /app/node_modules
    command: npm run dev
    restart: unless-stopped
